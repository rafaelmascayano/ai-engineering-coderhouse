[project]
name = "ai-engineering-coderhouse"
version = "0.7.0"
description = "Entregas acumulativas de AI Engineering: orquestador multi-agente jerárquico expuesto como API de producción con Redis, HITL y observabilidad."
requires-python = ">=3.12"

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC"]

[tool.mypy]
python_version = "3.12"
files = ["cyclic_agent"]
strict = true
warn_unreachable = true
show_error_codes = true
